import 'package:flutter/material.dart';

class DisasterControlScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('🌋 Disaster Control')),
      body: Center(child: Text('Disaster Module - Coming Soon', style: TextStyle(fontSize: 24))),
    );
  }
}